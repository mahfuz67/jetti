node_modules/

# build output
dist/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.turbo/

# browser-test artifacts (screenshots, snapshots, console logs)
.playwright-mcp

.env
*.log
# Scratch runs at logs/ are ignored; the committed submission artifact lives in
# logs/samples/ (LOG_DIR=logs/samples npm run batch) and is intentionally tracked.
logs/*.jsonl
!logs/samples/
examples/web/logs/
keys/
*.pem
.DS_Store

# grant submission artifacts (not project source)
claude-session.jsonl
codex-session.jsonl
grant-application.md

# architecture doc is hosted separately (gist); kept locally, not in the repo
ARCHITECTURE.md
